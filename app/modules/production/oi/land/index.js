import { render } from './view.js';
import { bind } from './ctrl.js';

// Карточка ОИ «земельный участок» внутри модуля «Жилое здание (дом)».
export const card = {
  id: 'land',
  init() {},
  render,
  bind,
};
