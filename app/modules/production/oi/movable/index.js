import { render } from './view.js';
import { bind } from './ctrl.js';

// Карточка ОИ «движимое имущество» внутри модуля «Гражданское здание».
export const card = {
  id: 'movable',
  init() {},
  render,
  bind,
};
