// Карточка ОИ «земельный участок» — импортируется из land-plot (там она
// самая полная и обновляется для всех модулей разом). Сознательное
// исключение из изоляции модулей — см. app/README.md и граф знаний
// (decision: карточка земельного участка (land-plot) — взята версия v2).
//
// Прежняя локальная реализация (view.js и ctrl.js рядом) удалена 08.09.2026:
// в трёх модулях она разошлась между собой — 10, 10 и 3 поля, — а в apartment
// её не было вовсе, и поиск по коду находил поля, которых в интерфейсе давно
// нет (отчёт покрытия §3.2). Если понадобится вернуть — она в истории git.
import { render } from '../../../land-plot/oi/land/view.js';
import { bind } from '../../../land-plot/oi/land/ctrl.js';

export const card = {
  id: 'land',
  init() {},
  render,
  bind,
};
