# Граф знаний проекта

Проект использует MCP-сервер памяти (`@modelcontextprotocol/server-memory`)
как граф знаний: сущности (entities) с наблюдениями (observations) и связи
(relations) между ними. Он покрывает и архитектуру/код, и рабочий контекст —
задачи, решения, договорённости, открытые вопросы.

Работаем командой через git, в нескольких ветках одновременно, поэтому
граф специально устроен так, чтобы **не мержился как обычный файл** —
см. «Как это живёт в git» ниже. Это первое, что нужно понять, прежде чем
что-либо туда писать.

## 1. Установка окружения (один раз на машину)

1. Проверить Node.js: `node --version`. Если не установлен —
   `winget install --id OpenJS.NodeJS.LTS -e --scope user`
   (`--scope user` — без прав администратора; на Linux/macOS — обычный
   установщик/менеджер версий, `npx` должен быть на PATH).
2. После установки winget обновляет PATH только в реестре — **текущие
   открытые терминалы/окно VS Code его не увидят**. Если `node`/`npx` не
   находится сразу после установки — перезапустить терминал или сделать
   VS Code: `Developer: Reload Window`.
3. Конфиг сервера — `.mcp.json` в корне репозитория (уже в git, ничего
   доустраивать не нужно). Он ссылается на переменную
   `MEMORY_FILE_PATH=.claude/knowledge-graph/memory.jsonl` — это **локальный
   кэш**, не то, что хранится в git (см. раздел 3).
4. При первом открытии проекта Claude Code спросит разрешение доверять
   MCP-серверу `memory` из `.mcp.json` — подтвердить.
5. Сразу после клонирования/`git pull`, до первого использования графа,
   собрать локальный кэш из общего лога:
   ```
   python .claude/knowledge-graph/tools/rebuild_memory.py
   ```
   Без этого шага `memory.jsonl` может отсутствовать или быть неполным —
   сервер ничего не потеряет (создаст файл заново), но не будет знать
   того, что накопили другие ветки/участники.
6. Проверить, что инструменты сервера видны (`create_entities`,
   `read_graph`, `search_nodes`, ...). Если после перезапуска их всё ещё
   нет — см. «Восстановление после сбоя MCP» в конце файла.

## 2. Когда и что писать в граф

- **В начале нетривиальной задачи** — `search_nodes`/`open_nodes` или
  `read_graph`, чтобы вспомнить, что уже известно о затрагиваемых модулях,
  конвенциях, открытых вопросах, прежде чем изучать код с нуля.
- **После значимого изменения или решения** — новыми
  `create_entities`/`add_observations`/`create_relations`, и тем же самым
  содержимым — в новый файл лога (раздел 3). Не писать то, что тривиально
  следует из чтения кода (это ищется грепом), — только то, что дорого
  восстанавливать: причины решений, договорённости, открытые вопросы,
  нестандартные связи между частями системы, задачи в работе.
- Обычные добавления/уточнения — без вопросов пользователю. Спрашивать
  только при удалении существующих узлов/связей или явном противоречии с
  тем, что уже в графе.

### Таксономия entityType

- `Project` — сам проект (`inside`).
- `KernelModule` — файлы `app/kernel/*` и сам `app/kernel`.
- `Shell` — `app/shell`.
- `Page` — `app/pages/*`.
- `DomainModule` — модуль типа ОЦ в `app/modules/*`.
- `DomainConcept` — предметные понятия (ОЦ, ОИ, ЕНИ, МЕХ и т.п.).
- `Convention` — архитектурное правило, проверяемое на ревью.
- `Decision` — принятое и обоснованное решение.
- `OpenQuestion` — нерешённый вопрос / пункт «на согласование».
- `Tool` — вспомогательные скрипты.
- `Task` — текущая задача/договорённость по ходу работы.

Имя узла кода — путь от корня репозитория (`app/modules/apartment`,
`app/kernel/boot.js`). Для не-файловых понятий — короткое человекочитаемое
имя; для решений/вопросов/конвенций — префикс `decision:`/`open:`/
`convention:` (см. существующие узлы).

### Архитектурные правила проекта (см. также `app/README.md`)

- `modules/*` → `kernel` — можно; `modules/*` → `modules/*` — **нельзя**;
  `kernel|shell|pages` → `modules/*` — **нельзя**, кроме `kernel/registry.js`.
- Ядро не знает ни одного типа ОЦ и ни одного вида ОИ.
- Пять модулей ОЦ (`apartment`, `residential-house`, `civil`, `production`,
  `land-plot`) полностью изолированы друг от друга.

Если предлагаемое изменение нарушает эти правила — предупредить явно,
прежде чем реализовывать.

## 3. Как это живёт в git — лог вместо файла состояния

**Проблема:** MCP-сервер каждый раз перезаписывает `memory.jsonl` целиком.
Если бы этот файл был в git, две ветки почти гарантированно конфликтовали
бы на merge — построчный diff двух независимо переписанных версий не
разрешается автоматически. Держать граф синхронным между ветками через сам
`memory.jsonl` не получится.

**Решение (event log вместо общего файла):**

- `.claude/knowledge-graph/memory.jsonl` — локальный кэш каждого
  разработчика/машины. **В git не попадает** (см. `.gitignore`). Это
  быстрый индекс, который в любой момент можно пересобрать с нуля.
- `.claude/knowledge-graph/log/*.json` — **источник истины, коммитится в
  git**. Каждый файл — маленькая неизменяемая запись: что нового узнали
  (сущности/связи) в рамках одной сессии/ветки/решения. Файлы никогда не
  редактируются после коммита — только добавляются новые.
- Поскольку у каждой записи гарантированно уникальное имя (см. ниже), при
  merge между ветками git видит только добавление новых файлов —
  **конфликтов не бывает**, независимо от того, что происходит в
  `modules/*` или где угодно ещё в коде.
- Локальный кэш восстанавливается детерминированно replay'ем всех файлов
  лога по порядку (сортировка по имени = по времени): дубликаты сущностей
  сливаются (наблюдения объединяются без повторов), дубликаты связей
  схлопываются. Поэтому реплей идемпотентен — гонять его можно сколько
  угодно раз, в любом порядке подключения веток.

### Именование файлов лога — без коллизий

Формат: `<UTC-время>_<ветка>_<автор>_<случайный суффикс>.json`, например
`20260821T035343Z_kartochka-zhilogo-zdaniya_nikitaosminnov_4b84d1.json`.

Требования, зачем именно так:
- **Только ASCII** — кириллица в имени файла (в ветках вроде
  «карточка-жилого-здания») по-разному отображается в терминалах, `git
  status` (escaping путей) и некоторых редакторах — транслитерация снимает
  проблему полностью.
- Время + ветка + автор + случайный суффикс — практически исключает
  совпадение имён даже при одновременной работе нескольких человек в одной
  ветке в одну секунду.

**Не создавать эти файлы руками с произвольным именем** — использовать
генератор:
```
python .claude/knowledge-graph/tools/new_log_entry.py
```
Скрипт сам берёт текущую ветку/автора из git, транслитерирует, добавляет
случайный суффикx и создаёт файл-заготовку с пустыми `entities`/
`relations` — их нужно заполнить (тем же содержимым, что ушло в
`create_entities`/`create_relations` через MCP) и закоммитить вместе с
остальными изменениями по задаче — как обычный файл в PR/коммите, никакого
отдельного шага выгрузки не требуется.

### Инструменты (`.claude/knowledge-graph/tools/`)

- `new_log_entry.py` — создать новую запись лога с гарантированно уникальным
  именем (см. выше).
- `rebuild_memory.py` — пересобрать локальный `memory.jsonl` из всех файлов
  `log/*.json`. Запускать после `git pull`/merge/checkout ветки. Безопасно
  запускать в любой момент — детерминированно и идемпотентно.
- `dump_readable.py` — человекочитаемый дамп текущего графа (группировка по
  типу) в текст, без зависимости от Node/MCP — доступно любому в команде,
  даже без Claude Code:
  ```
  python .claude/knowledge-graph/tools/dump_readable.py > graph.txt
  ```

### Что делать при завершении задачи / перед PR

1. Всё новое, что стоит сохранить (решения, задачи, изменения архитектуры),
   уже должно быть добавлено в MCP-граф по ходу работы (`create_entities` и
   т.д.) — раздел 2.
2. Сгенерировать файл лога: `python .claude/knowledge-graph/tools/new_log_entry.py`.
3. Заполнить его `entities`/`relations` тем, что добавлено на этой ветке
   (просмотреть через `dump_readable.py` или `read_graph`, если не помнишь
   точный список).
4. Закоммитить файл лога вместе с остальными изменениями ветки.
5. После merge других веток — `rebuild_memory.py`, чтобы подтянуть их
   записи в свой локальный кэш.

### Периодическая уборка (не обязательно, но не забывать)

Лог растёт бесконечно. Раз в какое-то время (например, после мержа в
`main`) можно сделать «чекпойнт» — файл с полным текущим `read_graph()`,
помеченный как контрольная точка, — и договориться в команде, какие старые
файлы лога после этого можно архивировать/удалить (только на `main`, только
когда все ветки с несмерженными файлами лога это учли — иначе их контекст
потеряется). Это ручное решение команды, автоматически не делать.

## Восстановление после сбоя MCP

Если инструменты сервера памяти недоступны в сессии (например, не
подхватился PATH после установки Node.js) — `.claude/knowledge-graph/memory.jsonl`
можно читать/править напрямую: формат — одна JSON-строка на сущность/связь,
`{"type":"entity",...}` или `{"type":"relation",...}`. Изменения в него не
обязаны попадать в лог сразу — но не забыть занести их туда до коммита
(раздел 3), иначе они не переживут `rebuild_memory.py`.
